export default function DatasetsPage() {
  return (
    <div className="mx-auto max-w-3xl p-8">
      <h1 className="text-xl font-semibold text-neutral-50">Datasets</h1>
      <p className="mt-2 text-sm text-neutral-400">
        Not implemented yet. This will surface structured datasets available to the SQL tool once a live
        Postgres instance is populated — no fake data shown in the meantime.
      </p>
    </div>
  );
}
